// Frontend entry point - no business logic yet
import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <div>Mini Jarvis - Frontend placeholder</div>
    </React.StrictMode>,
)
